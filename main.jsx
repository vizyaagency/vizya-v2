import React from "react";
import ReactDOM from "react-dom/client";
import VizyaSelector from "./VizyaSelector";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VizyaSelector />
  </React.StrictMode>
);